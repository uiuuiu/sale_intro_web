.main-inner
  .container
    .row
      .span12
        .widget
          .widget-header
            span
            h3 Quản lý bài  Dịch Vụ
          .widget-content
            = form_for @post, url: admin_service_posts_path, html: {class: "form-horizontal", method: :post} do |f|
              fieldset
                .control-group
                  label.control-label for="table_code"
                    | Tiêu đề
                  .controls
                    = f.text_field :title, class: "span6"
                .control-group
                  label.control-label for="table_name"
                    | Danh mục
                  .controls
                    = f.select :service_category_id, @categories.map{|m| [m.name, m.id]},  class: "span6"
                .control-group
                  label.control-label for="table_name"
                    | Ảnh bài viết
                  .controls
                    = f.file_field :avatar, class: "span6"
                .control-group
                  label.control-label for="table_name"
                    | Mô tả
                  .controls
                    = f.text_field :description, class: "span6"
                .control-group
                  label.control-label for="content"
                    | Nội dung
                  .controls
                    = f.cktext_area :content, class: "span6", ckeditor: {height: 600}
                .form-actions
                  = f.submit "Tạo mới", class: "btn btn-primary"
                  = link_to "Huỷ", admin_service_posts_path, class: "btn"
